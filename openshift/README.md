# Using with Kubernetes

```bash
kubectl apply -f back-deployment.yaml,back-service.yaml,data-persistentvolumeclaim.yaml,db-persistentvolumeclaim.yaml,heatmaploader-deployment.yaml,iip-deployment.yaml,iip-service.yaml,imageloader-deployment.yaml,keys-persistentvolumeclaim.yaml,mongo-deployment.yaml,mongo-service.yaml,pathdb-deployment.yaml,pathdb-service.yaml,segloader-deployment.yaml
```
